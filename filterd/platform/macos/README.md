# macOS adapter (stub)

Production path: Apple **Network Extension** (`NEDNSProxyProvider`) with signing, notarization, and user approval.

Hackathon prototype (not in this tree yet): point system DNS at the local filterd listen address — labeled non-production only.

See the root implementation plan: shared `core` first, native adapter second.
