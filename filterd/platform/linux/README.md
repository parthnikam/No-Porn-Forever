# Linux adapter (stub)

Target for a later phase: Ubuntu + systemd + NetworkManager/systemd-resolved + nftables by daemon UID.

Do not claim “all distros.” Start with Ubuntu `.deb` and a systemd unit that runs `filterd run`.
